version: '3.8'

services:
  multi-agent-system:
    build: .
    container_name: multi-agent-chat
    stdin_open: true # Allows for interactive input (-i)
    tty: true        # Allocates a pseudo-TTY (-t)
    volumes:
      - ./outputs:/app/outputs # Mount outputs directory